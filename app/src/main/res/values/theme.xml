<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.Messenger" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary branding color -->
        <item name="colorPrimary">@color/dark_elements</item>
        <item name="colorPrimaryVariant">@color/dark_elements</item>
        <item name="colorOnPrimary">@color/black</item>

        <!-- Status bar color -->
        <item name="android:statusBarColor">@color/dark_elements</item>
        <item name="android:windowLightStatusBar">true</item>

        <!-- EditText style -->
        <item name="colorControlActivated">@color/black</item>
        <item name="colorControlNormal">@color/black</item>
    </style>

</resources>